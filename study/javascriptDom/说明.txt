01-循环
02-先执行一次，再判断条件循环
03-for循环
04-函数的用法
05-return的用法
06-获得一个元素节点的属性值，比如class,title,id,等属性的值
07-增加一个元素节点的属性值（或者修改）
08-显示一个节点的值，比如该节点的第一个子节点，就是标签 里的内容
09-显示该节点的第一个子节点内容
10-让页面加载完之前，执行多个函数脚本
11-增加或者修改一个标签里的内容
12-创建一个元素节点，标签，文本等，并把这些附加给一个元素的子节点
13-把一个节点，移动到某个节点之前